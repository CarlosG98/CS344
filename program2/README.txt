how to compile:

	included in zip file is a Makefile. enter the following command in terminal: make
	this should create an executable called "movies_by_year"
	to run, enter: ./movies_by_year

	in the event the makefile does not create an executable, enter following command: gcc --std=gnu99 -g movies_by_year main.c
	then run the executable

	